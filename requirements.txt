# Cerebellar SDC Extraction System - Requirements
# Python 3.8+

# Core Dependencies
flask==3.0.0
flask-cors==6.0.0
anthropic==0.72.0  # Citations work with search_result mode (Files API not yet in released SDK)

# PDF Processing - Using Marker (primary) + pdfplumber (advanced features)
marker-pdf==1.10.1
pypdfium2==4.30.0  # Required by Marker
PyPDF2==3.0.1
pdf2image==1.16.3
Pillow==10.3.0
pdfplumber==0.11.0  # Re-added for advanced table extraction and character-level coordinates
pytesseract==0.3.10  # OCR fallback for scanned PDFs
PyMuPDF>=1.24.0  # For annotated PDF export with highlights

# Note: Marker is primary for text extraction, pdfplumber for specialized features:
# - Marker: Structure preservation, markdown formatting, automatic section detection
# - pdfplumber: Advanced table extraction, character-level coordinates, precise bounding boxes

# Data Processing
python-dateutil==2.8.2
jsonschema==4.20.0

# Utilities
python-dotenv==1.0.0
requests==2.32.4

# Optional - For Advanced Features
# numpy==1.24.3
# pandas==2.0.3
# opencv-python==4.8.0.76  # For advanced image processing
